<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style for a tab that displays a category name -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/tabIndicatorColor</item>
        <item name="tabSelectedTextColor">@color/tabIndicatorColor</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
        <item name="tabBackground">@color/primary_color</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/CategoryTabTextColor</item>
        <item name="android:background">@color/primary_color</item>
    </style>
    
    <!-- Style for description text view -->
    <style name="DescriptionTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/description_text_view_layout_marginBottom</item>
        <item name="android:layout_marginLeft">@dimen/description_text_view_layout_marginLeft</item>
        <item name="android:layout_marginRight">@dimen/description_text_view_layout_marginRight</item>
        <item name="android:drawablePadding">@dimen/description_text_view_drawablePadding</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">@dimen/description_text_view_textSize</item>
    </style>

</resources>
